import { V6CustomPipePipe } from './v6-custom-pipe.pipe';

describe('V6CustomPipePipe', () => {
  it('create an instance', () => {
    const pipe = new V6CustomPipePipe();
    expect(pipe).toBeTruthy();
  });
});
